# 📑 Звіт про Лабораторну роботу №5

## 🎯 Тема та Мета Роботи

| | |
| :--- | :--- |
| **Тема:** | Узагальнені типи (Generics), колекції і LINQ, обробка винятків. |
| **Мета:** | Набути практичних навичок зі створення узагальнених класів (`Repository<T>`), використання колекцій та LINQ для обчислень, а також коректної валідації даних та обробки власних винятків. |

---

## 📚 Опис Реалізації

### 1. Узагальнений Репозиторій (Generics)
* Реалізовано інтерфейс **`IRepository<T>`** та клас **`Repository<T>`**. Це дозволяє зберігати будь-які сутності (в нашому випадку `Receipt`) в універсальному контейнері, підвищуючи гнучкість коду.
* Для зберігання даних використано узагальнену колекцію **`Dictionary<Guid, T>`**.

### 2. Структура Даних та Обчислення (Композиція та LINQ)
* **Композиція:** Клас **`Receipt`** (Чек) містить колекцію об'єктів **`LineItem`** (Товарні позиції).
* **Обчислення:** В класі `Receipt` використано LINQ (`Sum`) для розрахунку `Subtotal`, а також реалізовано логіку нарахування **ПДВ (20%)** та **знижки (5%)** при сумі чека понад 2000 ₴.
* **LINQ у `Program.cs`:** Використано методи `Sum`, `Average` та `Where` для агрегованого аналізу колекції чеків.

### 3. Обробка Винятків
* Створено власні винятки: **`InvalidItemException`** та **`NotFoundException`**.
* **Валідація:** `InvalidItemException` генерується у setter-ах властивостей `Price` та `Quantity` (`LineItem`) при введенні негативних або нульових значень.
* **Обробка:** В `Program.cs` та `Repository.cs` використано блоки `try-catch` для коректного перехоплення винятків.

## 💡 Висновок

Лабораторна робота виконана успішно. Набуто практичних навичок використання узагальнених типів для створення універсального репозиторію, ефективного використання LINQ для роботи з колекціями, а також реалізації надійного коду через валідацію вхідних даних та обробку власних винятків.

---

## 📦 Крок 2: Завантаження на GitHub

Після того, як ви **зберегли** `README.md` та скріншот **`lab5_output.png`** у папці `lab5v1`:

1.  **Поверніться** до батьківської папки **`OOP-DENUS_B`**:
    ```bash
    cd ..
    ```
2.  **Завантажте** всі зміни (всі 5 файлів .cs, README.md, скріншот):
    ```bash
    git add .
    git commit -m "Lab5: Composition + Generics + Exceptions. Додано звіт та фінальний код."
    git push
    ```

**Вітаю! Лабораторна робота №5 повністю завершена.**