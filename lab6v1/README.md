# 📑 Звіт про Лабораторну роботу №6: Лямбда-вирази, анонімні функції та делегати

## 🎯 Тема та Мета Роботи

| | |
| :--- | :--- |
| **Тема:** | Лямбда-вирази, анонімні функції та делегати у C#. |
| **Мета:** | Набути практичних навичок роботи з делегатами, лямбда-виразами, анонімними методами та їхнім застосуванням у вбудованих делегатах (`Func`, `Action`, `Predicate`) та LINQ-операціях для обробки колекцій. |

---

## 📚 Реалізація та Використані Концепції (Варіант 1: Product)

Проєкт реалізує клас **`Product`** (Id, Name, Price, Category) та використовує функціональні конструкції для обробки колекції товарів.

### 1. Делегати та Анонімні Методи

* **Власний Делегат:** Оголошено делегат `public delegate bool PriceCheck(decimal price);` для перевірки цінових умов.
* **Анонімний Метод:** Використаний для демонстрації старого синтаксису делегатів при фільтрації товарів за ціною (`price < 5000.00m`).

### 2. Лямбда-вирази та Вбудовані Делегати

Всі вбудовані делегати реалізовані за допомогою **лямбда-виразів** (`=>`):

| Делегат | Призначення | Приклад Лямбда-виразу |
| :--- | :--- | :--- |
| **`Func<T, TResult>`** | **Обчислення середньої вартості:** Приймає `List<Product>` і повертає `decimal`. | `list => list.Average(p => p.Price)` |
| **`Predicate<T>`** | **Перевірка найдорожчого:** Приймає `Product` і повертає `bool` (перевірка `p.Price == maxPrice`). | `p => p.Price == maxPrice` |
| **`Action<T>`** | **Вивід у консоль:** Приймає `string` і не повертає значення. | `s => Console.WriteLine($"[INFO] {s}")` |

### 3. LINQ та Обробка Колекцій

Лямбда-вирази є ключовими для LINQ-операцій над колекцією `products`:

* **Фільтрація (`Where`):** `products.Where(p => p.Price > 10000.00m)`
* **Проєкція (`Select`):** Створення анонімного типу з ціною та ПДВ: `.Select(p => new { p.Name, PriceWithVAT = p.Price * 1.2m })`
* **Агрегація (`Aggregate`):** Обчислення загальної суми товарів: `products.Aggregate(0m, ...)`

## 💡 Висновок

Лабораторна робота виконана успішно. Набуто практичних навичок використання сучасних функціональних можливостей C# — **лямбда-виразів** та вбудованих делегатів. Це дозволило реалізувати ефективний та лаконічний код для фільтрації, обчислень та агрегації даних колекції, повністю задовольнивши вимоги завдання.