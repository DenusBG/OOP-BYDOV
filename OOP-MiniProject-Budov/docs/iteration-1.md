# Звіт за ітерацію №1

## Що вже працює:
* Створено архітектурний каркас (4 шари + тести).
* Реалізовано базові сутності: `Book`, `Magazine`, `Reader`.
* Готовий перший вертикальний зріз (Реєстрація читача -> Видача книги через `LibraryService` -> Оновлення стану в `InMemoryRepository`).

## Ризики та плани на Lab 35:
* Зараз дані зникають після закриття консолі. На наступній ітерації додамо збереження через `System.Text.Json`.